# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
# Required by POST /api/corpus/upload, which takes a multipart file. FastAPI
# raises at IMPORT time without it, so the whole API fails to start rather than
# just that one endpoint - it is not optional.
python-multipart==0.0.20

# LLM providers (all optional at runtime - the default `heuristic` provider
# needs none of them; import failures are handled gracefully).
anthropic>=0.42.0

# Optional providers - uncomment if you set KIVI_LLM_PROVIDER accordingly.
# groq>=0.11.0
# openai>=1.59.0
# google-genai>=0.3.0
# sentence-transformers>=3.3.0
